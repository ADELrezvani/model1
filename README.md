# BMI Calculator (PyQt6 + SQLite)

Enter name, height (cm) and weight (kg); the app validates the input with regex,
stores it in SQLite, then shows the BMI.

## Run

```
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
python main.py
```
